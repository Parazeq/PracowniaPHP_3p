SELECT nazwa, cena, opis FROM abonamenty;

SELECT ROUND(AVG(cena), 2) AS Srednio_abonament FROM abonamenty;

SELECT a.nazwa, c.cecha FROM abonamenty a JOIN SzczegolyAbonamentu s ON a.id = s.Abonamenty_id JOIN Cechy c ON s.Cechy_id = c.id WHERE a.id = 1;

SELECT nazwa, opis FROM abonamenty WHERE opis LIKE '%zdrowie%' OR opis LIKE '%opieka%';