SELECT tytul, plik FROM zdjecia WHERE polubienia >= 100;

SELECT z.plik, z.tytul, z.polubienia, a.imie, a.nazwisko FROM zdjecia z JOIN autorzy a ON z.autorzy_id = a.id ORDER BY a.nazwisko ASC;

SELECT a.imie, COUNT(z.id) AS liczba_zdjec FROM zdjecia z JOIN autorzy a ON z.autorzy_id = a.id GROUP BY a.imie;

ALTER TABLE zdjecia ADD rozmiarPliku INT;